# forlan/ — Workspace Pointer

קובץ זה הוא **pointer לבני אדם בלבד**. Claude Code לא טוען אותו.

## הסוכן הקנוני
ראה `.claude/agents/forlan.md`. Claude Code סורק רק קבצים flat ב-`.claude/agents/`, לכן ההגדרה חייבת לשבת שם.

## תיקייה זו (`forlan/`)
Workspace בלבד:

- `reference/` — תמונות השראה (drop here). פורלאן יסרוק אוטומטית.
- `outputs/` — תוצרים. כל `.png` מלווה ב-`.txt` עם הפרומפט ששימש.
