# forlan/ — Skill Pointer

הסקיל שמייצר את התמונות יושב ב-`.claude/skills/gpt-image-gen/SKILL.md`.

קובץ זה הוא pointer לבני אדם — Claude Code סורק רק `.claude/skills/<name>/SKILL.md`, לא קבצים בשורש הפרויקט.

הסוכן `forlan` קורא לסקיל הזה לכל יצירת תמונה. אל תקרא ל-OpenAI API ישירות.
